// randomly sets image speed and which index to strat with first
image_speed = random_range(0.5,1);
image_index = random_range(0,6);

// for going in random direactions
move_x = random_range(-2,2);
move_y = random_range(-2,2);

// for scaling
image_xscale = choose(1,-1);
image_yscale = choose(1,-1);